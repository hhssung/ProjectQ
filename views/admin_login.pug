extends layout

block content
    h1= "관리자 로그인 페이지"

    p
    p ㅎ.ㅎ
    p

    form(action= temp + "/admin/login", method="post")
        div
            input(type='text' name='id' placeholder= "admin ID")
        div
            input(type='password' name='password' placeholder='password')
        div
            input(type='submit' value='Login')